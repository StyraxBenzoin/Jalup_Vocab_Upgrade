@font-face { 
  font-family: "Noto Sans";
  src: url("_NotoSansJP-Regular.ttf"); 
  font-weight: normal;
}

.card {
  font-family: "Noto Sans", sans-serif;
  font-size: 18px;
  text-align: center;
  color: #546e7a;
  background: #fffcfc;
}

.card.nightMode { background: #292734; }

h1 {
  font-size: 28px;
  font-weight: normal;
}

h2 {
  font-size: 20px;
  font-weight: normal;
}

.box {
  padding: 10px 15px;
  background: white;
  border-radius: 10px; /* Rounded corners */
  box-shadow: 0 0 15px 0 rgba(0,0,0,.1);
}

.nightMode .box {
  background: #1c1b23 ;
}

#card-id {
  font-size: 12px;
  color: #cfd8dc;
  float: right;
  margin-top: -2px;
  margin-right: -5px;
}

.nightMode #card-id { color: #6d4aff; }

.divider {
  background-color: #eceff1;
  height: 1px;
  width: 100%;
}

.nightMode .divider {
  background-color: #7662cb;
}

.vocabulary-definition .example, .note {
  font-size: 16px;
  color: #83808f;
}

/*
 * makes ruby text unselectable (and hence uncopyable)
 * - it makes the text unselectable, but the goal was to make the text also uncopyable
 * - however, the text remains copyable for some reason in anki
 *   WHEN this is combined with the clipboard inserter plugin
 * - text is not copyable in modern browsers like firefox
 */
ruby rt {
  user-select: none;
}

::-webkit-scrollbar {
    display: none;
}
